import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    roadmapTitle: {
        listStyleType: "none"
    },
    skillLevelBlock: {
        textAlign: 'start',
        marginLeft: '30px',
        listStyleType: 'none'
    },
    technologyTitle: {
        marginLeft: '30px',
        listStyleType: "none"
    }
}))